<%= form_for(@playdate_guest) do |f| %>
  <% if @playdate_guest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playdate_guest.errors.count, "error") %> prohibited this playdate_guest from being saved:</h2>

      <ul>
      <% @playdate_guest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :playdate_id %><br />
    <%= f.number_field :playdate_id %>
  </div>
  <div class="field">
    <%= f.label :dog_id %><br />
    <%= f.number_field :dog_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
